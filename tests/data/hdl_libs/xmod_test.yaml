name: xmod_test

functions:
  xmod_test:
    # The filename is not necessary if same as modname.
    filename: xmod_test
    modname: xmod_test
    nargs: 2
    args:
      $0: A
      $1: B
      C: "$!u8=17"
      $RESULT: XOUT

